[pytest]
minversion = 7.0
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --cov=api --cov-report=term-missing --cov-report=html --tb=short
markers =
    integration: integration tests
    unit: unit tests
    slow: slow running tests
